# PyTorch ставится ОТДЕЛЬНО в run.bat командой с --index-url cu128 (поддержка
# RTX 50xx / Blackwell, sm_120). cu124 НЕ поддерживает эти карты и тянет CPU-сборку.
# Вручную (GPU): pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# CPU-only:      pip install torch torchvision

# Компьютерное зрение
opencv-python>=4.10
numpy>=1.26

# Глубокое обучение
transformers>=5.0           # RT-DETRv2 (детекция баблов) + GLM-OCR (glm_ocr, OCR)
huggingface_hub>=0.24       # скачивание anime-big-lama
# Сегментация текста — Comic Text Detector (ONNX, специализирован для манги)
# Веса (~30 MB) скачиваются с HuggingFace при первом запуске автоматически.
onnxruntime-gpu>=1.18      # GPU-инференс; замените на onnxruntime если нет CUDA

# Изображения и шрифты
Pillow>=10.4

# HTTP (Ollama API)
requests>=2.32

# Веб-интерфейс
fastapi>=0.110
uvicorn[standard]>=0.29     # ASGI-сервер + поддержка WebSocket
python-multipart>=0.0.9     # парсинг multipart/form-data для загрузки файлов
